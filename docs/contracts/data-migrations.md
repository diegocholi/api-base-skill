# Migrações

Este contrato foi consolidado em `docs/migrations.md`.

Resumo do contrato:

- Mudança de schema exige migração versionada.
- `migrations:check` deve passar no PR.
- Deploy deve executar migração antes do rollout.

Referência canônica:

- `docs/migrations.md`
